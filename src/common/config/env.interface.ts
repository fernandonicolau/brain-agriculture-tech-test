export interface EnvironmentVariables {
  PORT: number;
  NODE_ENV: string;
  DATABASE_URL?: string;
  DB_HOST: string;
  DB_PORT: number;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_NAME: string;
}
